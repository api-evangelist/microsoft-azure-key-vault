{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/CertificateUpdateParameters",
  "title": "CertificateUpdateParameters",
  "type": "object",
  "description": "The certificate update parameters.",
  "properties": {
    "policy": {
      "$ref": "#/components/schemas/CertificatePolicy"
    },
    "attributes": {
      "$ref": "#/components/schemas/CertificateAttributes"
    },
    "tags": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      },
      "description": "Application specific metadata in the form of key-value pairs.",
      "example": "example_value"
    }
  }
}